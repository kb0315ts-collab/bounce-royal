# 솔로 로그라이크

작업 브랜치: `claude/roguelike` (`main` 5ec37e8에서 갈라짐). **배포하지 않은 로컬 전용 모드다.**

혼자 20웨이브를 넘는 모드. 한 번 지면 런이 끝나고, 20웨이브(현 아레나 챔피언)를 이겨야 클리어다.
웨이브마다 제한 시간 없이 적을 모두 쓰러뜨리면 끝나고, 끝날 때마다 보상을 고른다.

## 로컬에서 해 보기

```bash
cd bounce-royal-roguelike
node server/index.js
```

브라우저로 `http://localhost:8080` → 타이틀의 **솔로 로그라이크**.
주소 뒤에 `?dev=1`을 붙이면 로비 맨 아래에 **테스트용 바로가기**가 생겨 원하는 웨이브부터 시작한다
(그 앞 웨이브들은 이긴 것으로 치고 AI 기준으로 증강을 받아 둔다).

흐름은 로비 → 무기 선택 → 곧바로 W1 전투 → 보상 → 다음 웨이브다. 웨이브 목록이나 적 구성을
미리 보여 주는 화면은 없다. 선택형 웨이브에서만 약한/보통/강한 적 중 무엇과 겨룰지 고른다
(어떤 적이 나오는지는 알려 주지 않고, 보상 크기만 알려 준다).

### 몬스터 전시관

`http://localhost:8080/monster-hall.html` — 몬스터 15종을 한 마리씩 무대에 세우고 실제 게임과 같은
두뇌·그림으로 움직인다. 왼쪽에서 고르고, 느리게 보기·인형 고정으로 모션을 뜯어보고,
보스는 패턴 단추로 원하는 공격을 바로 시킬 수 있다(2페이즈도 켤 수 있다).
로비에서 `?dev=1`을 켜면 바로 가는 링크가 생긴다. 게임 흐름과는 완전히 분리된 화면이다.

- 진행 중인 런은 브라우저에 저장된다(`bounce-royale-rogue-run-v1`). 로비의 **이어하기**로 돌아온다.
- 전투 중 `Esc`/`P` 또는 오른쪽 위 버튼으로 일시정지. 탭을 숨기면 자동으로 멈춘다.
- 일시정지의 **런 포기**는 버튼을 두 번 눌러야 한다.

## 구성

| 웨이브 | 종류 |
|---|---|
| 1 · 7 · 13 · 19 | 라이벌전 — 같은 라이벌과 네 번. 1:1 아레나 전투 |
| 3 · 6 · 9 · 12 · 15 · 18 | 선택형 — 약한 적(기본 스탯 +10% 하나) · 보통(증강 1회) · 강한 적(증강 2회) |
| 10 | 중간보스 — 늙은 전 챔피언 (해머 노장) |
| 20 | 최종보스 — 현 아레나 챔피언 (빠른 마법사) |
| 나머지 | 일반 몬스터전 (16 후반, 17 후반 고난도) |

- **라이벌**: 첫 만남에서 캐릭터·무기가 정해지고 런 내내 같다. 증강 1 → 9(무기 1) → 16(무기 2) → 25(무기 3)개로,
  자기 무기와 잘 맞는 쪽으로 빌드가 자란다. 첫 만남에서는 해설이 '라이벌'이라고 부르지 않는다.
- **고블린볼 5종**: 몽둥이 · 점액(맞거나 웅덩이를 밟으면 굼떠짐) · 거인 · 해머 정예 · 마법사.
  근접 셋(몽둥이·거인·해머)은 **아레나의 검·단검처럼 무기를 몸에 달고 돌린다** — 예고도 휘두르는 동작도 없이
  닿으면 맞고, 한 번 닿은 뒤에는 무기에서 떨어졌다 다시 닿아야 또 맞는다. 거인은 밀어내고, 해머는 크게 날려
  벽에 박으면 추가 피해와 기절을 건다(밀어내기는 쿨다운이 있다 — 닿을 때마다 날리면 손쓸 틈이 없었다).
  예고하고 치는 것은 보스와 러시혼처럼 특별한 몬스터만 한다. 마법사·점액은 원거리라 쏘기 전 짧은 동작이 있다.
- **기믹 8종**: 볼트윈(둘 사이 전기줄) · 흡착볼(벽에 세게 부딪혀야 떨어짐) · 붐볼(카운트다운 폭발, 다른 몬스터에게도 피해·연쇄) ·
  드릴볼(벽 속으로 들어가 금 간 자리로 튀어나옴) · 멀티젤(1→2→4→8, 한 가족 8마리까지) · 러시혼(겨누고 돌진, 피하면 벽에 박혀 빈틈) ·
  스파크젤(꺼짐 → 예고 → 전기장) · 메딕볼(주변 몬스터 회복).
- **보스 예고 원칙**: 모든 광역 공격은 자리를 표시하고 충분히 예고한 뒤 터진다. 조향이 초당 50도라서,
  예고는 '그대로 달리면 빠지는 것'과 '궤도를 틀면 빠지는 것'으로만 만든다(앞질러 찍어서 피할 수 없는 공격은 없다).
  체력 50%에서 2페이즈(예고가 조금 짧아지고 패턴이 이어진다).
- **증강**: 아레나 전용 11종(코인·연승·패배·연장전·1:1 승패 전제)은 보상과 라이벌 빌드에서 뺐다. 로그라이크 전용 증강은 없다.
- **선인장 해설**: 게임이 남긴 이벤트(`b.rogueEvents` — `RIVAL_APPEAR_1~FINAL`, `FORMER/CURRENT_CHAMPION_APPEAR`,
  `VOLTTWIN_LINK_CREATED`, `SUCTIONBALL_ATTACHED`, `BOOMBALL_COUNTDOWN`, `DRILLBALL_BURROW`, `MULTIGEL_DUPLICATED`,
  `RUSHHORN_CHARGE`, `SPARKGEL_FIELD_ON`, `MEDICBALL_HEAL` …)만 읽는다. 우선순위는 이번 런에서 처음 본 기믹 > 강한 위험 > 특이한 결과 > 평범한 반복.
  평범한 반복은 한두 번 말하고 조용해진다. 몬스터 웨이브에서는 내 스킬 적중·크게 맞음·위기만 따로 말한다.
  보상 화면에서는 방금 웨이브를 한 줄로 돌아본다(다음 상대가 누구인지는 말하지 않는다).
  라이벌·챔피언 소개는 그 전투가 시작될 때 처음 나온다. 20웨이브를 넘으면 GG 무대가 뜬다.

## 파일

| 파일 | 내용 |
|---|---|
| `js/rogue-data.js` | 웨이브·몬스터·보스·라이벌·보상 수치. **밸런스는 여기서만 만진다** |
| `js/rogue-sim.js` | 몬스터·보스 두뇌, 예고(hazard), 넉백·벽꽝, 런 저장/복원, 헤드리스 자동 진행 |
| `js/rogue-render.js` | 몬스터·보스 외형, 예고 표시, 투사체, 초상화 |
| `js/rogue.js` | 로비 · 갈림길(난이도 선택) · 전투 HUD · 보상 · 일시정지 · 결과 화면 |
| `css/rogue.css` | 화면 스타일 |
| `monster-hall.html` · `js/monster-hall.js` · `css/monster-hall.css` | 몬스터 전시관 (게임과 분리된 점검용 화면) |
| `tests/rogue.test.js` | 규칙 테스트 14개 |

공용 파일에는 **PvP에 영향이 없는 갈고리만** 넣었다(`sim.js`: `opts.rogue`가 있을 때만 도는 훅, 몬스터 `brain`,
`phased`, 넉백, 제한 시간 없음 등). 아레나 수치는 하나도 바꾸지 않았다. 멀티 서버는 `sim.js`를 같이 읽지만
로그라이크 파일은 불러오지 않는다. 소리 42개는 `js/audio.js`의 '로그라이크' 묶음(청음실에도 보인다).

## 밸런스 손잡이 (`js/rogue-data.js`)

- `ROGUE_RIVAL_MEETS[].power` — 라이벌 체력·피해 배율(0.7 → 0.8 → 0.8 → 0.82). 라이벌은 늘 주인공보다 증강이 많고
  1:1은 캐릭터·무기 상성이 커서, 이 배율 없이는 라이벌전 네 번이 런을 거의 다 끝냈다.
- `ROGUE_MONSTER_WEAPON_DMG` — 몬스터전에서만 쓰는 무기 배율(방패 2.0 · 쇠사슬 1.9 · 지뢰 1.35 · 화염 1.4).
  이 무기들은 몬스터 상대로 초당 피해가 절반쯤이었다. 라이벌전과 아레나에는 걸리지 않는다.
- 근접 몬스터의 `rot`(초당 회전) · `reach`(길이) · `tip`(굵기) · `dmg`(닿을 때 피해) · `knock`/`knockCd`(밀어내기).
  회전이 빠를수록 자주 닿는다. 몽둥이는 주인공보다 느려서 잡히지 않는 편이고, 거인·해머가 실제 위협이다.
- 웨이브별 `hp`·`dmg` 배율, 몬스터별 수치, 보스 패턴별 `warn`(예고 시간)·`r`(범위)·`dmg`.

헤드리스로 잰 현재 모습(주인공 AI 솜씨 0.7, 웨이브마다 증강 1개를 받은 빌드, 판당 40회):
일반·선택형(보통)은 대체로 75~100%, 선택형(강함)은 40~80%, 라이벌 W1 ~80% · W7 ~57% · W13 ~38% · W19 ~35%,
W10 보스 ~35%, W20 보스 ~33%. 런 100번을 끝까지 돌리면 4번쯤 클리어하고, 많이 죽는 자리는 W1·W5·W7·W10이다. 봇은 사람처럼 예고를 미리 읽지 못하므로
(자동 진행 주인공에게만 늦고 가끔 놓치는 회피를 흉내 내 넣었다) 사람이 하면 보스·후반이 더 수월할 것이다.
실제로 해 본 뒤 위 손잡이로 맞춘다.

## 아직 남은 것

- 실제 플레이 감각에 맞춘 밸런스 조정(특히 W7·W10·W19·W20).
- 로그라이크 전용 증강은 기획대로 아직 만들지 않았다.
- 커밋·배포 전이다. 올릴 때는 `main`에 합치기 전에 전체 테스트(`node --test tests/*.test.js`)를 돌린다.
