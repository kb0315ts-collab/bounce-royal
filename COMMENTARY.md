# 우주 스포츠 해설자

작업 브랜치: `codex/cactus-caster`.
이번 외형·음성 변경의 복구 기준: `674ef96` (기존 로봇 해설자).
해설자 추가 전 복구 기준: `230a6e0` (사용자가 조정한 GAME_SPEED와 서버 틱 수정 포함).
기존 우주 스포츠 비주얼만 있는 기준은 `b301969`.

## 동작

- 전투 중 좌상단의 말하는 선인장 장난감과 웹툰 말풍선. 게임의 공·무기와 같은 굵은 외곽선, 단순한 색면의 원본 SVG다. 현재 보는 전투만 중계하며 타이틀 영상과 일반 메뉴에는 등장하지 않는다.
- 닉네임·무기 소개, 기존 2연승 이상, 실제 스킬 적중, 같은 상대에게 2초 안에 3회 이상 연속타, 번개/연쇄 번개/충격파 적중, 체력 비율 격차 35%p 이상을 해설한다.
- 공격 스킬은 실제 HP 피해가 확인돼야 적중으로 말한다. 방어·강화 스킬 등은 발동으로 표현한다. 출혈/화염 지속 피해나 동시 다중 투사체를 연속타로 과장하지 않는다.
- 일반 해설은 실제 시간 3.5초 간격, 동일 특수 공격/스킬은 8초 간격. 해설은 밀어두지 않고 지난 상황은 버린다.
- 캐스터를 누르면 천 입마개를 씌운다. 일반 말풍선은 사라지고 간헐적으로 짧은 `웁…!` 몸짓만 한다. 다시 누르면 해제된다. 선택은 브라우저에 저장된다.
- 각 라운드 종료는 승리·무승부를 평범한 해설로 알린다. 4인 난투 중 한 명만 쓰러지거나 내 전투만 끝났다고 GG를 띄우지 않는다.
- **전체 게임의 최종 승패가 결정되는 마지막 타격·시간 판정 순간**, 경기장이 보이는 상태에서 큰 `GG~~!`를 표시한다. 실제 정산 규칙으로 최종 종료가 확인돼야 하므로 코인 보호, 남은 분열체, 아직 진행 중인 다른 전투가 있으면 성급하게 외치지 않는다. 입마개를 쓴 상태라면 천이 갈라지며 한 번 외친다. 약 2.5초 뒤 연출이 사라지고 기존 입마개 선택은 그대로 유지된다. 결과 화면으로 넘어가도 다시 외치지 않는다. 결정 순간을 받지 못한 구형 서버·재접속 등에는 결과 화면이 예비 경로다.
- 말할 때 원본 비언어 음절을 짧게 이어 재생한다. 각 음절에 맞춰 입이 열렸다 닫히고 몸통이 화분 위에서 살짝 흔들린다. 타 게임 음성을 사용하지 않는다. 기존 전투 효과음은 변경하지 않았다.
- 입마개는 일반 해설 텍스트·음성을 모두 끈다. `웁…!`는 조용한 몸짓만 한다. 최종 GG는 입마개 예외지만, 게임 전체 소리 끄기·볼륨 설정은 항상 따른다. 소리 미리듣기는 `sound-lab.html`의 **선인장 해설자 · 조잘조잘** 항목이다.
- 동작 줄이기 환경에서는 흔들림을 끈다. 탭을 숨기거나 메뉴로 나가면 해설과 음성을 즉시 정리하며, 돌아와도 지나간 대사를 몰아 재생하지 않는다.

## 구조 및 안전성

- `js/sim.js`: 확정된 적중/스킬 사실만 관전용 이벤트로 기록. 최근 2초/최대 96건. 별도 순번을 사용해 전투 난수·엔티티 ID·규칙·피해·속도를 바꾸지 않는다. target=0은 대상 없는 스킬 발동이다.
- `js/match-conclusion.js`: 플레이어·전투 복사본에 기존 라운드 정산 함수를 적용해 최종 종료만 미리 확인한다. 원본 코인·순위·조향·타이머는 바꾸지 않고 난수도 소비하지 않는다. 로컬 `Game.update`와 서버 `Room.tick`에서 결과가 확정된 첫 프레임에 한 번 확인한다.
- `server/snapshot.js`, `js/net.js`: 선택적 `ce` 배열과 연승 정보, 최종 종료 시 `mc` 정보를 전투 결과와 같은 스냅샷에 전달한다. 이벤트가 없는 구형 서버에서도 기존 전투는 작동한다.
- `js/commentary-core.js`: 화면과 독립적인 해설 우선순위·중복 방지·관전 전환 처리. 보간된 화면 시점보다 미래인 이벤트는 시점이 도달할 때까지 소비하지 않는다. 일반 `observe`는 라운드 결과만, `decideMatch`는 최종 GG를 만든다. 결과 화면의 `finishMatch`도 같은 중복 방지 키를 사용한다.
- `js/commentary.js`, `css/commentary.css`, `index.html`: 안전한 텍스트 DOM, 인라인 선인장 SVG와 작은 CSS 동작. 닉네임을 HTML로 삽입하지 않는다. 음절·입 모양은 짧게 살아 있는 동일한 UI 프레임 시계를 사용한다.
- `js/ui.js`: 로컬/서버 공용 결과 화면에서 예비 `finishMatch`를 호출한다. 이미 시작된 GG는 재생을 다시 시작하지 않고, 전투 HUD가 닫힌 뒤에도 결과 화면 위에서 남은 시간만 표시 후 자동 정리된다.
- `js/audio.js`: 기존 Web Audio 믹서에 낮은 우선순위의 `caster.chatter` 음성만 추가했다. 전용 난수 상태로 전투 난수와 분리하며, 입마개는 해설 음성만 멈춘다.
- `assets/caster/cosmic-caster.png`: 이전 로봇 캐릭터는 복구용으로 보관하지만 현재 화면에서는 로드하지 않는다. 새 외형은 별도 이미지 다운로드가 필요 없다.

## 검증

```sh
node --test --test-concurrency=1 tests/*.test.js
node tools/check-caster-browser.js <playwright-module-path> <screenshot-directory> [base-url]
```

두 번째 검사는 로컬 서버가 필요하다(기본 localhost:8080). 실제 친선방/서버 스냅샷,
320·390·720px 화면 배치, 실제 차지 샷 적중, 음성·입 모양 연동,
입마개 토글/무음/유지, 평범한 라운드 결과, 코인 정산 전 결정 프레임의 GG 파열 및 결과 전환 중 중복 방지,
전투 프레임 없이 최종 연출 자동 정리, 메뉴 숨김, JS/HTTP 오류를 검사한다.

속도 기준은 `230a6e0`을 그대로 유지한다. 전투 규칙·수치·시뮬레이션 속도·실제 정산 시점은 변경하지 않는다. GG 타이밍 변경 전 복구 기준은 `e3c1f74`이다.
원격 브랜치를 강제 덮어쓰거나 다른 에이전트의 로컬 main 작업 트리를 변경하지 않는다.
