# 우주 스포츠 해설자

작업 브랜치: `codex/cosmic-caster`.
복구 기준: `230a6e0` (사용자가 조정한 GAME_SPEED와 서버 틱 수정 포함).
기존 우주 스포츠 비주얼만 있는 기준은 `b301969`.

## 동작

- 전투 중 좌상단 로봇 캐스터와 웹툰 말풍선. 현재 보는 전투만 중계하며 타이틀 영상과 메뉴에는 등장하지 않는다.
- 닉네임·무기 소개, 기존 2연승 이상, 실제 스킬 적중, 같은 상대에게 2초 안에 3회 이상 연속타, 번개/연쇄 번개/충격파 적중, 체력 비율 격차 35%p 이상을 해설한다.
- 공격 스킬은 실제 HP 피해가 확인돼야 적중으로 말한다. 방어·강화 스킬 등은 발동으로 표현한다. 출혈/화염 지속 피해나 동시 다중 투사체를 연속타로 과장하지 않는다.
- 일반 해설은 실제 시간 3.5초 간격, 동일 특수 공격/스킬은 8초 간격. 해설은 밀어두지 않고 지난 상황은 버린다.
- 캐스터를 누르면 천 입마개를 씌운다. 일반 말풍선은 사라지고 간헐적으로 짧은 `웁…!` 몸짓만 한다. 다시 누르면 해제된다. 선택은 브라우저에 저장된다.
- 현재 보는 전투의 최종 결과가 나오는 순간, 입마개를 터뜨리며 큰 `GG~~!` 말풍선. 약 1.4초 후 원래 음소거 상태로 돌아온다. 4인 난투 중 한 명만 쓰러진 경우에는 종료 GG를 띄우지 않는다.
- 텍스트 해설이며 새 음성/효과음은 없다. 기존 게임 효과음 설정과 별개다. 동작 줄이기 환경에서는 흔들림 애니메이션을 끈다.

## 구조 및 안전성

- `js/sim.js`: 확정된 적중/스킬 사실만 관전용 이벤트로 기록. 최근 2초/최대 96건. 별도 순번을 사용해 전투 난수·엔티티 ID·규칙·피해·속도를 바꾸지 않는다. target=0은 대상 없는 스킬 발동이다.
- `server/snapshot.js`, `js/net.js`: 선택적 `ce` 배열과 연승 정보를 전달한다. 이벤트가 없는 구형 서버에서도 기존 전투는 작동한다.
- `js/commentary-core.js`: 화면과 독립적인 해설 우선순위·중복 방지·관전 전환 처리. 보간된 화면 시점보다 미래인 이벤트는 시점이 도달할 때까지 소비하지 않는다.
- `js/commentary.js`, `css/commentary.css`: 안전한 텍스트 DOM과 작은 CSS 동작. 닉네임을 HTML로 삽입하지 않는다. 이미지도 전투 진입 시에만 불러온다.
- `assets/caster/cosmic-caster.png`: 생성한 전용 투명 캐릭터(약 1.13 MiB). 제작 프롬프트는 같은 폴더 README 참조.

## 검증

```sh
node --test --test-concurrency=1 tests/*.test.js
node tools/check-caster-browser.js <playwright-module-path> <screenshot-directory>
```

두 번째 검사는 localhost:8080 서버가 필요하다. 실제 친선방/서버 스냅샷,
320·390·720px 화면 배치, 실제 차지 샷 적중, 입마개 토글/유지,
GG 파열 후 복원, 메뉴 숨김, JS/HTTP 오류를 검사한다.

속도 기준은 `230a6e0`을 그대로 유지한다. 이번 배포는 그 기존 속도 수정도 포함한다.
원격 브랜치를 강제 덮어쓰거나 다른 에이전트의 로컬 main 작업 트리를 변경하지 않는다.
